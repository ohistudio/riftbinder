RIFTBINDER — RAW CLAD PROMPT LOG
================================

Every prompt typed to the agent over the build, verbatim and in order, extracted
from the Claude Code session transcript. Nothing corrected, nothing grouped —
spelling, interruptions and mid-task messages are as they were sent. Images the
person attached (reference screenshots, a Rift Atlas decklist) are marked
[image attached].

The agent's replies and its tool activity — file edits, Lens Studio calls, LEAF
runs, screenshots — are not reproduced here; there are several thousand of
them. docs/CLAD-PROMPTS.md is the curated version of this list, grouped by what
was being worked on and traced to the code each prompt produced.

Redacted: one Riot API key pasted early on, and UUIDs.

98 prompts.

[2026-09-01 07:42 UTC]
load clad up

[2026-09-01 07:45 UTC]
# Binder — a spatial collection scanner and deckbuilder for SPECS

Unofficial fan project for Riftbound: League of Legends Trading Card Game.
Drop this at the repo root. Claude Code reads it as the project contract.

## What we are building

A Lens for Snap Spectacles that reads your physical Riftbound cards through the
camera, builds a spatial collection wall from what you actually own, and lets you
build a deck by voice against that collection — then hands you a decklist and a
pull list for finding the cards in the box.

The problem it solves is not "deckbuilding is hard." Moxfield-style builders are
fast and good. The problem is that every digital builder assumes you already
typed in your collection, which nobody does, and that a finished list on your
phone still leaves you hunting forty cards in a shoebox. The cards are physically
in front of you and the glasses can see them. That is the entire reason this
belongs on a headset and not a laptop.

## Hard IP constraints — read before writing any feature

This project lives or dies by Riot's Riftbound digital tools policy. Violating
any of these is not a style question.

- **No gameplay.** No automated rules enforcement, no turn tracking, no life or
  score counters, no play simulation. Deckbuilding and collection management only.
  Riot explicitly encourages the latter and prohibits the former.
- **Card assets come from the Riot API only.** No scraping fan card databases, no
  copying art or text from third-party sites. Until the API key is approved, use
  the local fixture set described below.
- **Prices — allowed, deferred.** Market prices are third-party market data, not a
  Riftbound asset, so the API-only rule does not cover them. The binding
  constraints come from the price feed's own terms. Out of v1 for scope reasons,
  not legal ones. Build behind `PriceSource`.
- **Metagame data — probably allowed, confirm first.** Riot's prohibition on
  metagame-defining data (deck and card play rates, win rates, matchup
  differentials) appears in a list scoped to apps that simulate or replicate
  gameplay, which this does not. The scoping is ambiguous and keys are granted at
  Riot's discretion, so confirm through the Developer Support Platform before
  shipping it, and keep it out of the initial key application. Build behind
  `StatsSource`.
- **Monetisation is possible** for an app that never simulates gameplay, holds an
  approved key, keeps a free tier, charges only for transformative content, and
  has no betting or gambling. Never any crypto or non-fiat currency.
- **Attribution.** The Lens and the README must both display the fan-project
  notice from Section 6 of Riot's Legal Jibber Jabber, copied verbatim from
  https://www.riotgames.com/legal. Do not paraphrase it.
- **Official rules only.** If the deck validator implements a Riftbound format,
  it must match the official construction rules exactly. No custom banlists, no
  house formats presented as official.

Keep all card data behind `CardSource` (below) so swapping the fixture set for the
real API is a one-file change when the key lands.

## Platform

- Lens Studio 5.22+, SPECS project, TypeScript.
- Camera frames via Camera Module; vision and text calls via Remote Service
  Gateway. Token in `RemoteServiceGatewayCredentials`, **never committed**.
- No npm dependencies.
- World-space units in this document are **centimetres**; convert at the Lens
  Studio boundary.

---

## Data schema

```ts
export type Domain = string;        // sourced from card data, never hardcoded
export type CardType = 'unit' | 'spell' | 'gear' | 'rune' | 'legend' | 'battlefield';

export interface Card {
  id: string;                       // `${setCode}-${collectorNumber}`
  name: string;
  setCode: string;
  collectorNumber: string;
  type: CardType;
  domain: Domain | null;
  cost: number | null;
  text: string;                     // official English text, verbatim
  tags: string[];
}

/** Everything card-data related goes through this. Fixture now, Riot API later. */
export interface CardSource {
  all(): Card[];
  byId(id: string): Card | null;
  /** Name-normalised index for fuzzy matching scan results. */
  search(query: string, limit: number): { card: Card; score: number }[];
}

/**
 * Deferred data sources. Both ship as null implementations in v1: every method
 * returns null and the UI hides the corresponding fields entirely. Adding real
 * prices or meta stats later is then a new implementation plus a config flag,
 * not a refactor. Never let either source influence deck validation.
 */
export interface PriceSource {
  /** Market price for a specific printing, in minor units. Null if unknown. */
  forPrinting(cardId: string): { amount: number; currency: string; asOf: number } | null;
  attribution(): string | null;     // feed's required credit line, rendered if present
}

export interface StatsSource {
  forCard(cardId: string): { playRate?: number; asOf: number } | null;
  attribution(): string | null;
}

export interface CollectionEntry {
  cardId: string;
  count: number;
  firstScannedAt: number;
}

export interface ScanResult {
  rawName: string;                  // exactly what the vision model read
  rawCollectorNumber: string | null;
  confidence: number;               // 0..1, from match score not the model
  matched: string | null;           // Card.id, null if below threshold
  alternatives: string[];           // up to 3 Card.ids, for disambiguation
}

export interface Deck {
  id: string;
  name: string;
  legendId: string | null;
  main: { cardId: string; count: number }[];
  runes: { cardId: string; count: number }[];
  battlefieldIds: string[];
  updatedAt: number;
}

export interface Violation {
  rule: string;                     // e.g. 'main.minimumSize'
  message: string;
  severity: 'error' | 'warning';
}
```

### Deck validation

`validate(deck, source): Violation[]`.

**Do not hardcode the construction rules from memory.** Riftbound uses a main deck
plus a separate rune deck, a champion legend, and battlefields, with sizes and
copy limits set by format. Pull the authoritative numbers from the Riot API
ruleset or the official rules document and put them in `Rules.ts` with a source
comment and a date. If a number cannot be verified, mark the rule
`severity: 'warning'` and label it unverified in the UI rather than asserting it.

---

## Identification pipeline

The single most important architectural decision: **do not track cards
continuously, and do not train a model.**

Every card is the same rectangle, so shape tracking tells you nothing about
identity, and identity needs reading, not detection. A 1,200-class classifier is
weeks of work and breaks on every set release.

Instead:

1. **Scan gate.** A world-anchored frame, roughly 20cm tall, floating at a
   comfortable holding distance. The user holds a card into it. UI is anchored to
   the gate in world space, never to the card, which removes the tracking problem
   entirely.
2. **Trigger** on a still-hold: frame-to-frame difference under threshold for
   400ms, or the `SCAN` voice intent.
3. **Vision call** through Remote Service Gateway. Send the cropped gate region.
   Prompt asks for JSON only: `{ name, collectorNumber, setCode }`, transcribing
   the printed text exactly, with nulls where unreadable. No guessing, no
   knowledge of the game — pure transcription.
4. **Match locally.** Exact collector number plus set code wins outright. Otherwise
   normalised Levenshtein on the name against `CardSource.search`. Threshold 0.82
   for auto-accept; below that, surface the top 3 as alternatives and let the user
   say "the second one".
5. **Cache** by collector number. A rescan of a known card is instant and
   increments `count`.

Prefer the collector number over the name wherever it is legible — the name
identifies a card, the number identifies the printing.

Unrecognised scans go to a visible "unmatched" tray, never silently dropped.

---

## Spatial layout

Two walls plus a focus slot. All auto-arranged; the user never drags anything.

```
collection wall   240 x 160 cm, anchored left, 250 cm from user
deck wall         180 x 160 cm, anchored 40 deg to the right of the collection wall
tile              7.2 x 10 cm  (card aspect), 2 cm gutter
grid              20 cols x 10 rows = 200 visible tiles, voice-paged
focus slot        centred between the walls, card rendered 25 cm tall
scan gate         14 x 20 cm, 45 cm from the user, below the focus slot
```

**Legibility strategy — the focus card, not distance LOD.** Card rules text cannot
be usefully abbreviated, so do not try. Tiles show art region, name, cost and
domain colour only. Gazing a tile renders that card at 25cm in the focus slot,
where the full official text is readable. One card readable at a time is correct
here; a wall of readable cards is not achievable and not needed.

Deck wall groups the main deck into columns by cost, so the curve is the layout
rather than a separate chart.

---

## Interaction

Gaze targets, voice acts, hands do almost nothing.

| Intent      | Utterances                                  | Needs gaze |
|-------------|---------------------------------------------|-----------|
| `SCAN`      | "scan", or automatic on still-hold          | no        |
| `ADD`       | "add", "add three"                          | yes       |
| `REMOVE`    | "remove", "take one out"                    | yes       |
| `FILTER`    | "show units under four", "only <domain>"    | no        |
| `CLEAR`     | "show everything"                           | no        |
| `PICK`      | "the second one" (scan disambiguation)      | no        |
| `EXPORT`    | "export", "save the list"                   | no        |

`FILTER` is parsed by an LLM call into a structured predicate over `Card` fields,
then applied locally. Never let the model choose cards — it parses the query, the
local code filters. Riftbound is newer than most model training data and the model
does not know these cards.

Hands: pinch-drag repositions a wall. Nothing else. No card dragging.

**Debug fallback, built in Phase 0.** Every intent fires from a tap panel with
identical payloads, and `SCAN` accepts a canned image. Voice and camera both fail
on demo day; the recorded video must be able to run without either.

---

## Export

Two artifacts, because both are the point:

1. **Decklist** — plain text, one card per line with counts, legend and runes
   separated. Must round-trip: an exported list re-imports to the same `Deck`.
2. **Pull list** — the same cards grouped by set and collector number in ascending
   order, which is the order you flip through a sorted box. This is the bridge back
   to the physical cards and it is the feature nobody else has.

---

## Build order

Do not start a phase before the previous one runs in Spectacles Preview.

- **Phase 0** — `Card`, `CardSource` with a hand-entered fixture set of ~20 cards,
  collection wall renderer, focus slot, tap debug panel. No camera, no AI.
- **Phase 1** — Scan gate: camera frame, still-hold trigger, vision call, fuzzy
  match, card flies to the wall. Canned-image path first, live camera second.
- **Phase 2** — Deck wall, `ADD`/`REMOVE` by gaze plus voice, cost-column layout.
- **Phase 3** — `validate()` against `Rules.ts`, violations surfaced on the deck wall.
- **Phase 4** — `FILTER` parsing and local filtering.
- **Phase 5** — Export decklist and pull list.
- **Phase 6** — Stretch: one frame containing several spread cards, all identified
  in a single vision call. Attempt only if Phases 0–5 are solid.

Phases 0–2 are the minimum viable demo. Phase 5 is what makes it a tool rather
than a toy.

## Tests

- `CardSource.search` returns the right card for exact names, for names with one
  character of OCR noise, and returns low scores for genuine non-matches.
- Collector-number match beats a conflicting name match.
- Scan below threshold produces alternatives and never auto-adds.
- Duplicate scan increments `count` rather than creating a second entry.
- Layout: no two tiles overlap; the grid stays inside wall bounds; paging is
  stable when the collection grows mid-session.
- `validate()` has one test per rule in `Rules.ts`.
- Decklist export round-trips to an identical `Deck`.
- Vision response parsing survives fenced JSON, trailing prose, and truncation.

## Non-goals

Gameplay of any kind — that one is permanent. Continuous card tracking. Trained
card-recognition models. Hand-dragged layout. More than one deck open at a time.
Multiplayer.

Deferred, not forbidden: prices and metagame stats. Both have interfaces defined
above and null implementations in v1. Do not build either this week, and do not
mention either in the Riot key application.

## Style

TypeScript, strict, no `any` in the schema layer. Layout, matching, and validation
are pure functions testable without the Lens Studio runtime. All tunable numbers —
wall dimensions, tile size, match threshold, still-hold duration — live in
`Config.ts`, because every one of them will change once it is on a face. RGAPI-[REDACTED]

[2026-09-01 08:09 UTC]
you fill it with random cards rengar etc

[2026-09-01 08:12 UTC]
RGAPI-[REDACTED] use it

[2026-09-01 08:54 UTC]
try again to get image we surely can get it

[2026-09-01 09:00 UTC]
cool can we increase quality? next i wanna work on or talk through a system that is like ml and object detection for cards so we can place ui on real cards

[2026-09-01 09:03 UTC]
you can go agasint the breif i sent the theme is Build a spatial tool that helps people create something faster, easier, or more intuitively. i want a spatial expierrence that encompases this so i think having an ai agent that you can talk to that suggests cards for your deck, a search feature to find cards, a deck builder. for tracking doesnt matter what card it is aslong as its a uniqye tracker. we can also use gemini etc. to identify real cards.

[2026-09-01 09:11 UTC]
do it

[2026-09-01 09:48 UTC]
you wanna have another go at quad detector?

[2026-09-01 09:57 UTC]
sp how do i see it working? i cant see what youve done in preview i just see 3 rows of cards

[2026-09-01 10:04 UTC]
how do i see the card trakcign youve made?

[2026-09-01 10:17 UTC]
so in real life this would work with cards even if they are help up right say flat abagainst a table. can we populate the preview with fake cards that i can test the trakcing with

[2026-09-01 10:24 UTC]
but it only tracks in that square right?

[2026-09-01 11:11 UTC]
yes

[2026-09-01 11:16 UTC]
done? seems to just put boxes infront of me? les focus on making it a complete build first so its a deck builder to fit the theme lets do that

[2026-09-01 11:28 UTC]
so this can get all cards right? lets first work on making the ui better its too much

[2026-09-01 11:32 UTC]
think you should look up how to build a deck like we need to start with the hero card?

[2026-09-01 11:41 UTC]
how do i cycle betweent he champion cards like maybe i dont want to play those 3?

[2026-09-01 11:48 UTC]
dunno i feel like we need a cleaner approach, we can locally save all champions right as theres not a huge amount. you get the rules for riftbound, https://riftbound.gg/builder/cvu7qmglo5/ use this as examples. i also like the tier list

[2026-09-01 12:00 UTC]
how do i pick a hereo/ champion card then - so start is pick a legend, pick a batttlefield note these cards are side ways pick 3. pick runes but rmemeber based on legend, legend, main deck, battleiflends, runes, Filter cards by color, type, energy cost, and more. Great for finding exactly what you need! Drag this divider left or right to adjust the panel sizes. Make more room for whichever side you need!Here's your deck! Cards you add will appear organized by type.Every deck needs a Legend! Pick one from the Legend tab to get started.Add 40 cards to your main deck. You can have up to 3 copies of each card.Don't forget 3 Battlefields and 12 Runes! The Auto button helps balance your runes.Sideboard

Store extra cards here for swapping between games. Right-click any card in your deck to move it here.The Bench

A scratchpad for cards you're considering! Park cards here while planning - they're saved with your deck.Sort Your Cards

Organize your deck view by energy, color, name, and more!
Deck Views

Check out Stats to see your energy curve, or Sample Hand to test draws!

[2026-09-01 12:36 UTC]
so first hover shouldnt select - pinch selects if we select we can press next to pick champion, runes, main deck etc.

[2026-09-01 12:42 UTC]
shouldn the cards relaod to chosen chosen champions etc. and so on

[2026-09-01 12:52 UTC]
can you render battleifrields better? also make sure the ofucsed one are sdieways how do i pick battlefiedls, pnch to lect and then pinch to deselect?

[2026-09-01 13:06 UTC]
this doesnt feel the right flow so im assuming you have the ai suggestions? lets make the whole ui better. so we have a grid this should be a panel on a Ui panel that allows masking and schrolling. it needs oto be easier then just pressing next or add, select is enough then you can slect again to deselect. Ai suggestions are still wanted

[2026-09-01 13:13 UTC]
can we do a find store nearby? events nearby etc.

[2026-09-01 13:26 UTC]
get stuck on runes as cant multi select cards also move ui buttons to the panel

[2026-09-01 13:31 UTC]
we dont need scroll up and down we can use masking, can make a nicer sort bit and add some feel to the buttons

[2026-09-01 13:36 UTC]
https://riftdecks.com/ can access this to show top decks?

[2026-09-01 13:41 UTC]
scroll is not working

[2026-09-01 14:03 UTC]
think zoom the cards in so 5 across , we dont need rmeove last, lets have ai as another side panel and the deck your amkeing as another one, dont need recentre

[2026-09-01 15:12 UTC]
the scroll is a bit sensitive, make the panels moveable and position them all correctly.

[2026-09-01 15:19 UTC]
i think always auto do runes, keep ask button, keep sort button, add a search button with a keyboard. and also add a camera button, this takes a pic, use gemini to understand the card and add to deck

[2026-09-01 15:29 UTC]
i want it to have a main menu, so it doesnt go straight into deck building. Can we like build deck, your cards. on your cards you can manually add or take a pic. and then last emnu item will be store finder.

[2026-09-02 05:50 UTC]
remove the binder was created text.the zoomed in card stll is not movemable i can move soemthing but it is not the card image.same with menu i move it but the buttons dont move with it. Use the Ui skills make the UI look better right now is very dark and bland. [Assets/Scripts/Binder/Runtime/BinderApp.ts:740] [Binder] scanning the live camera
[Assets/Scripts/Binder/Runtime/CardScanner.ts:110] [Binder][scan] read name=null number=null set=null are we using gemini?

[2026-09-02 06:00 UTC]
cant click on any of thebuttons, you have not chnaged the Ui style at all i cant see what you have done

[2026-09-02 06:07 UTC]
restyle the whole thing using UI kit

[2026-09-02 06:18 UTC]
the button bottom panel whould move with main panel, lets make it a nicer colour the panel is dark. for scan card are we sending camera frame to gemini?

[2026-09-02 06:27 UTC]
oh it seems to not be sending my camera frame - so for preview i have made fake images of cards in the scene i want it ot see the fake render not just real world

[2026-09-02 06:36 UTC]
[Image #5] im looking at this it seems to not show me what image it sends to gemini the image seems cut off like its clipping close to camera
[image attached]

[2026-09-02 06:36 UTC]
[Image: source: /Users/liammason/Documents/rengars.png]

[2026-09-02 07:28 UTC]
[Assets/Scripts/Binder/Runtime/CardScanner.ts:126] [Binder][scan] read name=Rengar number=UNL 120 set=null whys it say null we are adding it to our card collection it got it right

[2026-09-02 07:32 UTC]
no gemini overlay that i can see but it got the card i want osme ui like add to deck?

[2026-09-04 06:18 UTC]
out of intrest can we use leaf to spawn a hand and put one of those rengar cards in it. so in preview when we press scan card we doa leaf animation with a hand that has a rengar card in it withis one of the images in the scene panel

[2026-09-05 14:39 UTC]
open up the project

[2026-09-05 14:44 UTC]
{
  "mcpServers": {
    "lens-studio": {
      "headers": {
        "Authorization": "Bearer AJ6bnpU2-lRPTZUsguNBxH33WPuX1VPGwNkzADpxFb5n3PLhKvy2IQ0qM2rnf05z"
      },
      "type": "http",
      "url": "http://localhost:50040/mcp"
    }
  }
}

[2026-09-05 14:45 UTC]
commected?

[2026-09-05 14:47 UTC]
do it

[2026-09-05 15:04 UTC]
how do i run leaf?

[2026-09-05 15:06 UTC]
run it

[2026-09-05 15:07 UTC]
run it again

[2026-09-05 15:24 UTC]
[Image #8] i want our decks to show like this so after you create a deck it formats like this also think we need a your decks menu
[image attached]

[2026-09-05 15:24 UTC]
[Image: source: /Users/liammason/Downloads/deckksss.webp]

[2026-09-05 15:36 UTC]
next

[2026-09-05 15:47 UTC]
card prices just use gemni, do ask what your point at

[2026-09-05 15:58 UTC]
next

[2026-09-05 16:07 UTC]
next

[2026-09-05 16:21 UTC]
next

[2026-09-06 07:17 UTC]
do a run on the ui, use ui kits i dont like the current theme - add a nice gradient and also a border using mesh builder

[2026-09-06 07:28 UTC]
buttons are hard to see

[2026-09-06 08:15 UTC]
id like the button fill to have a color too

[2026-09-06 08:41 UTC]
seems the scan card isnt sending the environment too can you show me the leaf scnario for scan card again we want a hand to hodl the card up scan the card etc

[2026-09-06 08:54 UTC]
[Image: original 2333x1590, displayed at 2000x1363. Multiply coordinates by 1.17 to map to original image.]

[2026-09-06 09:15 UTC]
do it

[2026-09-06 09:40 UTC]
nexr

[2026-09-06 09:49 UTC]
[Image: original 2041x1590, displayed at 2000x1558. Multiply coordinates by 1.02 to map to original image.]

[2026-09-06 09:52 UTC]
[Image: original 2041x1590, displayed at 2000x1558. Multiply coordinates by 1.02 to map to original image.]

[2026-09-06 09:56 UTC]
[Image: original 2041x1590, displayed at 2000x1558. Multiply coordinates by 1.02 to map to original image.]

[2026-09-06 09:59 UTC]
[Image: original 2041x1590, displayed at 2000x1558. Multiply coordinates by 1.02 to map to original image.]

[2026-09-06 10:04 UTC]
stop moving the mnue if i move, seems we have two panels building the deck, i like th eone wiht the formatting i asked for deck sheet

[2026-09-06 10:10 UTC]
[Assets/Scripts/Binder/Runtime/BinderApp.ts:1465] [Binder] PRICE_CARD no price? and ask button doesnt seem to do anything?

[2026-09-06 10:34 UTC]
position the new deck panel closer, can you also build me a few decks so my decks is populated

[2026-09-06 10:42 UTC]
make me a logo for this too please

[2026-09-06 10:50 UTC]
i think for the deck sheet we can make them bigger we have alot of free space dont we also need main deck and then side deck? lets add ask button on desck sheet

[2026-09-06 10:57 UTC]
weres the side deck? youve also clipped the card titles. position the deck sheet similar to the other panels its close and to the right of the player right now

[2026-09-06 11:11 UTC]
the bottom panel that says no problems found overlaps wiht the ask about this deck button

[2026-09-06 11:17 UTC]
<local-command-stdout>Set model to `claude-fable-5-1[1m]` and saved as your default for new sessions</local-command-stdout>

[2026-09-06 11:17 UTC]
okie go through this and imporve what you think needs imporved i think the UI could look better, some sound effects, some feel/ jucie when interacting, hovering cards. see what else you find

[2026-09-06 11:35 UTC]
on the zoomed in card can we make the 1 enrgery mind etc stats its own little panel, o cant interact wiht the cards in the deck sheet page. lets also take a look at the leaf scnearios i wanted to just be able to record a leaf of it scanning both rengar cards then building a deck.

[2026-09-07 03:37 UTC]
add todo list some floating vfx, some vfx for snaning a card etc. i think we need a hotbar for saccing card in main menu too. continue

[2026-09-07 04:04 UTC]
run me the leaf

[2026-09-07 04:07 UTC]
this doesnt seem to work its not nice looking for recoridng you are too close to the menu it cuts it off , i wanna see the menu in the reocrding

[2026-09-07 04:10 UTC]
run leaf

[2026-09-07 04:15 UTC]
turn the card portrait. after scan hide the card, look full at the panel of the scan card, press ask, then price, then choose. make sure this panel is in view

[2026-09-07 04:20 UTC]
closer FIRST RANGAR CARDneeds to be  PORTRAIT right now its horizontal! Then when you look at the scanned card panel you need to look higher to include it all in the frame only do one rengar scan then start adding other cards to the deck after a few look at the deck sheet make sure its all in biew

[2026-09-07 04:23 UTC]
Ive told you multiple times - Rengar card is horizontal i want it portrait, look at the entierity of the ask panel right now we see the bottom half you need to look higher

[2026-09-07 04:25 UTC]
run

[2026-09-07 04:40 UTC]
deploy to github, do all of this Prepare your submission, which must include:
Public project repo link
Share a public link to your project repository.
Demo video
Submit a demo video using file sharing platforms like Google Drive, Dropbox or WeTransfer. Your video should include a walkthrough of your spatial experience, its key features, and how it uses CLAD.
CLAD prompt log
Include a text file, transcript, or document showing the prompts and AI-assisted workflow used to help create your project with CLAD.
Project description
Briefly describe what you built, how it responds to the weekly theme, and who it is for.

[2026-09-07 04:49 UTC]
Week 4: Create
Build a spatial tool that helps people create something faster, easier, or more intuitively.

[2026-09-07 04:51 UTC]
i have elevenlabs make me a minute long description of the project for the video

[2026-09-07 04:54 UTC]
no speech should be like This is RiftBinder — a spatial deckbuilder for the Riftbound card game, built for Snap Specs.

Building a deck usually means a browser, a spreadsheet, and a box of cards on the desk beside them. RiftBinder makes this easier spatially by x. It knows your collection and allows you to build decks, it even scans the scan infront of you allowing you to add to your digital cvery card lands on a sheet laid out like a real decklist: main deck, runes, sideboard. The rules check themselves as you go, and the deck tells you the moment it's legal.

And a friend in the same room can build it with you.

Every line of this was built with CLAD — the interface, the scanning, the assistant, and the simulated hand you've been watching, which is a LEAF scenario the agent wrote to test its own work.
ollection. you can ask questions about the card and even get its current makret value.

[2026-09-07 05:04 UTC]
all updated on github? /Users/liammason/Downloads/Riftbinder.mp4

[2026-09-07 05:08 UTC]
put video on github and we have the clad prompts?

[2026-09-07 05:11 UTC]
how do i link the prompt give me the link

[2026-09-07 05:13 UTC]
<task-notification>
<task-id>bqqc94ptc</task-id>
<tool-use-id>toolu_01SJ4HaevUNbpeS2AvAXWY5p</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-liammason-Documents-DeckBuilder/[uuid-redacted]/tasks/bqqc94ptc.output</output-file>
<status>failed</status>
<summary>Background command "Publish the video as a release asset" failed with exit code 1</summary>
</task-notification>

[2026-09-07 05:13 UTC]
how do i link the prompt give me the link

[2026-09-07 05:15 UTC]
try again

[2026-09-07 05:16 UTC]
<task-notification>
<task-id>b00r0slod</task-id>
<tool-use-id>toolu_01KuPuYDmGzgf2gvPHZpMnPc</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-liammason-Documents-DeckBuilder/[uuid-redacted]/tasks/b00r0slod.output</output-file>
<status>completed</status>
<summary>Background command "Upload the asset and publish the release" completed (exit code 0)</summary>
</task-notification>

[2026-09-07 05:18 UTC]
this is the raw prompt log? i did ask you to save it
